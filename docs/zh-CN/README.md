# UniAdapter

**不是框架。一个能在5分钟内为现有项目添加跨平台支持的开源工具。**

为你的React、Vue或任何JavaScript项目提供即时支持，覆盖Web、小程序、移动应用和Go分布式系统。

**[文档（中文）](./) | [文档（English）](../en) | [示例](./examples) | [贡献指南](./CONTRIBUTING.md) | [测试](./TESTING.md) | [品牌定位](./BRAND_POSITIONING.md)**

## 什么是UniAdapter？

UniAdapter是一个**平台适配层**，不是框架。它让你：

- ✅ 保持现有的技术栈（React、Vue、Svelte等）
- ✅ 无需重写代码即可添加跨平台支持
- ✅ 保持100%对项目结构的控制
- ✅ 同时部署到8个平台

### 支持的平台

- 🌐 **Web/H5**（Chrome、Firefox、Safari）
- 📱 **微信小程序**（wx.*）
- 🎬 **抖音小程序**（tt.*）
- ❤️ **小红书**（xhs.*）
- 🗺️ **高德地图**（AMap）
- ⚛️ **React Native**（原生桥接）
- 🔗 **Go分布式系统**（RPC、gRPC、消息队列）
- 🔶 **HarmonyOS**（OHOS API）

## 为什么UniAdapter与众不同

### 不是框架

```
❌ 你不需要学习新框架
❌ 你不需要重写项目
❌ 你不需要采用新模式
✅ 你只需添加一个适配层
```

### 纯粹集成

```typescript
// 你现有的React项目
import React, { useState } from 'react'
import { useUserAPI } from './hooks/useUserAPI'

export function UserList() {
  const [users, setUsers] = useState([])
  const api = useUserAPI()

  // 100% 你的代码，未做任何改变
  return <div>{/* 你的UI */}</div>
}

// 一行代码添加多平台支持
import { useUniRouter, useUniRequest } from 'uniadapter'

// 就这样。现在它可以在任何地方运行。
```

### 核心差异

```
           uniapp                          UniAdapter
        (框架)                          (适配层)
    ┌──────────────────┐            ┌──────────────────┐
    │  你的代码        │            │  你的React应用    │
    │  + uni.* API     │            │  (未改变)         │
    │  + 页面配置      │            └────────┬─────────┘
    │  + 清单文件      │                     │
    └────────┬─────────┘            ┌────────▼─────────┐
             │                      │  UniAdapter       │
    ┌────────▼─────────┐            │  useUniRouter()  │
    │ uniapp框架       │            │  useUniRequest() │
    └────────┬─────────┘            └────────┬─────────┘
             │                              │
    ┌────────▼──────────────────────────────▼─────────┐
    │      平台检测与适配                              │
    └──────────────────────────────────────────────────┘
             │
    ┌────────▼──────────────────────────────▼─────────┐
    │  Web │ 微信 │ 抖音 │ RN │ HarmonyOS │ 其他平台   │
    └───────────────────────────────────────────────────┘

关键差异：
- uniapp: 框架决定一切
- UniAdapter: 你的代码保持不变，我们只是适配
```

## 当前状态

**v1.2.0** - 生产就绪

- ✓ 98/98 测试通过（100%通过率）
- ✓ 8个平台完全支持
- ✓ TypeScript提供完整的类型定义
- ✓ 零运行时开销，支持tree-shaking
- ✓ 仅2KB gzipped（对比传统框架的50KB+）

## 快速开始

### 安装

```bash
npm install uniadapter
# 或
yarn add uniadapter
# 或
pnpm add uniadapter
```

### 基本使用

```typescript
import { detectPlatform, useUniState } from 'uniadapter'

// 平台检测
const platform = detectPlatform()
console.log(`当前平台：${platform.name}`)

// 跨平台状态管理
function Counter() {
  const [count, setCount] = useUniState(0)
  
  return (
    <div>
      <p>计数：{count}</p>
      <button onClick={() => setCount(count + 1)}>+</button>
    </div>
  )
}
```

## 高级功能

### 1. 跨平台路由
```typescript
import { useUniRouter } from 'uniadapter'

function Navigation() {
  const { push, replace, goBack } = useUniRouter()
  
  return (
    <>
      <button onClick={() => push('/home')}>首页</button>
      <button onClick={() => push('/profile')}>个人资料</button>
      <button onClick={() => goBack()}>返回</button>
    </>
  )
}
```

### 2. 统一网络请求
```typescript
import { useUniRequest } from 'uniadapter'

async function fetchData() {
  const request = useUniRequest()
  const response = await request.get('/api/users', {
    headers: { 'Content-Type': 'application/json' }
  })
  return response.data
}
```

### 3. 平台特定适配
```typescript
import { usePlatform } from 'uniadapter'

function PlatformSpecificComponent() {
  const { isWeChat, isDouyin, isWeb } = usePlatform()
  
  if (isWeChat) {
    return <WeChatSpecificUI />
  } else if (isDouyin) {
    return <DouyinSpecificUI />
  } else {
    return <WebDefaultUI />
  }
}
```

## 为什么选择UniAdapter？

### 对于中国开发者
1. **本土化支持**：深度集成微信、抖音、小红书等中国特色平台
2. **中文文档**：完整的中文文档和示例
3. **国内生态**：支持支付宝、高德地图等国内服务
4. **性能优化**：针对中国网络环境优化

### 对于全球开发者
1. **一站式解决方案**：一套代码，多平台部署
2. **零锁定**：随时可以移除，不影响现有代码
3. **渐进式采用**：可以从单个功能开始，逐步扩展
4. **社区驱动**：活跃的国际社区支持

## 项目路线图

### 近期计划（v1.3.0）
- [ ] 增强TypeScript类型支持
- [ ] 添加Next.js适配器
- [ ] 改进开发者工具
- [ ] 扩展文档和示例

### 中期计划（v2.0.0）
- [ ] 插件系统
- [ ] 可视化配置工具
- [ ] 性能监控系统
- [ ] 云服务平台

### 长期愿景
打造全球开发者首选的一站式跨平台解决方案，让每一行代码都能运行在任何平台。

## 贡献

我们欢迎所有形式的贡献！请查看[贡献指南](./CONTRIBUTING.md)了解更多信息。

### 开发流程
1. Fork项目
2. 创建功能分支（`git checkout -b feature/amazing-feature`）
3. 提交更改（`git commit -m '添加新功能'`）
4. 推送到分支（`git push origin feature/amazing-feature`）
5. 创建Pull Request

## 许可证

本项目基于[MIT许可证](./LICENSE)开源。

## 联系方式

- **邮箱**: 3578544805@qq.com
- **GitHub**: [liangfuliang541-pixel](https://github.com/liangfuliang541-pixel)
- **公司**: 福建省小南同学网络科技有限公司

## 特别感谢

感谢所有为UniAdapter做出贡献的开发者，你们的每一行代码、每一个问题和每一次讨论都让这个项目变得更好！

---

**UniAdapter - 让跨平台开发变得简单**