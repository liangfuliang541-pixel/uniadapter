# 📦 UniAdapter npm 发布配置指南

## 发布前准备

### 1. 版本更新
```bash
# 更新版本号
npm version patch  # 补丁版本
npm version minor  # 次要版本  
npm version major  # 主要版本
```

### 2. 构建检查
```bash
# 清理构建目录
rm -rf dist

# 构建库版本
npm run build:lib

# 验证构建产物
ls -la dist/
```

### 3. 发布测试
```bash
# 本地测试安装
npm pack
npm install ./uniadapter-1.1.0.tgz

# 验证功能
node -e "require('uniadapter')"
```

## 发布流程

### 1. npm 登录
```bash
npm login
# 输入用户名、密码、邮箱
```

### 2. 正式发布
```bash
# 发布到npm
npm publish

# 或发布测试版本
npm publish --tag beta
```

## 发布后验证

### 1. 安装验证
```bash
# 创建测试项目
mkdir test-project && cd test-project
npm init -y
npm install uniadapter

# 测试基本功能
node -e "console.log(require('uniadapter'))"
```

### 2. 功能测试
```javascript
// test.js
import { usePlatform, useUniState } from 'uniadapter'

console.log('Platform:', usePlatform())
const [state, setState] = useUniState('test')
console.log('State:', state)
```

## 版本管理策略

### 语义化版本
- **主版本** (1.x.x): 不兼容的API变更
- **次版本** (x.1.x): 向后兼容的功能新增
- **补丁版本** (x.x.1): 向后兼容的问题修正

### 发布标签
- `latest`: 稳定版本 (默认)
- `beta`: 测试版本
- `next`: 开发版本

## 自动化发布配置

GitHub Actions 已配置自动发布流程：
- 代码推送到 main 分支自动触发
- 通过测试后自动发布新版本
- 需要配置 NPM_TOKEN 秘钥

## 发布清单

- [ ] 版本号更新
- [ ] 构建产物验证
- [ ] 本地测试通过
- [ ] 文档更新
- [ ] CHANGELOG 更新
- [ ] npm 登录验证
- [ ] 发布后验证

## 常见问题

### 发布权限
确保 npm 账户有包的发布权限

### 版本冲突
检查 npm 上是否已存在相同版本

### 构建失败
确认所有依赖安装正确，TypeScript 编译通过

### 网络问题
使用稳定的网络环境，必要时配置 npm 镜像